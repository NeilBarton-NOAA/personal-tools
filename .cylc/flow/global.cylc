[install]
    [[symlink dirs]]
        [[[localhost]]]
            run = ${CYLC_WORKDIR}
[platforms]
    [[slurm_local]]
        job runner = slurm
        hosts = localhost
        install target = localhost
