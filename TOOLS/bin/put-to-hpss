#!/bin/sh
set -u
LOCAL_FILE=${1:-$PWD}
YEAR=${2:-1}
HTAR=${3:-F}

HSI_DIR=/NCEPDEV/emc-marine/${YEAR}year/Neil.Barton
if [[ ${HTAR} == T ]]; then
    hsi mkdir -p ${HSI_DIR}
    htar -cvf ${HSI_DIR}/test.tar ${LOCAL_FILE}/*
else
    hsi put ${LOCAL_FILE} : ${HSI_DIR}/${LOCAL_FILE}
fi
